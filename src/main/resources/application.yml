spring:
  application:
    name: dp

  profiles:
    active: dev   # 기본 실행환경을 'dev'로 설정 (MySQL 사용)

  # ================================
  # 🧩 공통 설정 (모든 프로필 공용)
  # ================================
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

# ================================
# 🧩 JWT 공통 설정
# ================================
jwt:
  secret: "abc123123abc123"

# ================================
# 🧩 DEV (MySQL - 로컬 개발 환경)
# ================================
---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: "jdbc:mysql://localhost:3306/club?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8"
    username: "root"
    password: "@Ab86256653#"
    driver-class-name: com.mysql.cj.jdbc.Driver

  h2:
    console:
      enabled: false

jwt:
  secret: "u283fh2hU93hf9283hF9283hf92hf983hf2h938hf293hf293hf"


# ================================
# 🧩 TEST (H2 - 테스트 환경)
# ================================
---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: "test-secret-key-123456"
